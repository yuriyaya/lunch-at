import styles from "./Log.module.css";

function LogStore() {
  return (
    <div>
      <input type="text" placeholder="식당이름 입력" />
      <button>다음</button>
    </div>
  );
}

export default LogStore;
