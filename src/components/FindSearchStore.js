import styles from "./Find.module.css";
import { Link } from "react-router-dom";

function FindSearchStore() {
  return (
    <div>
      <div className={styles.firstLevelDiv}>식당 검색(준비중)</div>
    </div>
  );
}

export default FindSearchStore;
